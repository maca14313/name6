import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.gmapmap',
  appName: 'mapgmapo',
  webDir: 'build',
  server: {
    androidScheme: 'https'
  }
};

export default config;
